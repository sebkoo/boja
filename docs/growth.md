# Growth

How Boja could grow, tied to decisions already made. Structure and hypotheses
only — no channel is claimed as working, and no numbers are invented. Every
funnel metric is a placeholder until it is instrumented (see
`docs/monetization.md`).

## Built-in distribution

The output is a plan card + `.ics` that is shareable anywhere and needs no
account from recipients (ADR-0003). The artifact itself is the main organic
surface: a recipient sees Boja's output before installing anything.

- Hypothesis: *(TODO)* shared plan card → recipient installs.
- Instrumentation: *(planned — funnel events in `docs/monetization.md`)*.

## Single-player value first

Boja is useful without a network of friends already on the app (ADR-0003), so
activation does not depend on inviting others. *(retention hypotheses: TODO)*

## Retention hooks

- Preference memory and reminders are the Pro surface (ADR-0003; README).
  *(planned; no retention claim until measured)*

## Channels (hypotheses only)

- App Store discovery — see `docs/aso.md`.
- *(TODO: others — none validated; do not claim a channel without data)*

## What this file will not claim

Positioning against other products is point-in-time and must be re-scanned
before any public claim (ADR-0003). No growth number appears here until it is
measured in this repo.

## Provenance

Traces to ADR-0003 (trust model, no-network, single-player) and the README. All
metrics are placeholders.
